export interface usersEdit {
    name: string;
    phone: string;
    address: string;
}

export interface instiEdit {
    name?: string;
    address?: string;
    phone?: string;
    email?: string;
    accountNumber?: string;
}